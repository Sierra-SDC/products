const { Products } = require('./products.js');
const { Styles } = require('./styles.js');
const { Features } = require('./features.js');

module.exports = {
  Products: Products,
  Styles: Styles,
  Features: Features,
};
